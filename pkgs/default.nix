# HACK: This file only exists so the flake picks up the pkgs directory for
# packagesFromDirectoryRecursive even when no custom packages are defined.
# Add real packages as pkgs/<name>/package.nix.
{ }
