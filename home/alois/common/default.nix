{ ... }:
{
  # User-specific configuration common across all of alois's hosts
  # This file is for settings that are specific to alois but shared across all hosts

  # Example: User-specific firefox profiles, paths, etc.
}
