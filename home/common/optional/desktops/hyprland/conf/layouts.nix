# ML4W layout configurations

{
  dwindle = {
    pseudotile = true;
    preserve_split = true;
  };

  master = {
    # new_status = "master"; # Commented due to compatibility
  };

  binds = {
    workspace_back_and_forth = false;
    allow_workspace_cycles = true;
    pass_mouse_when_bound = false;
  };
}
